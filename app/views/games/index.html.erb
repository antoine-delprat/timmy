<h1>Liste des jeux</h1>


<ul>
  <% @games.each do |game| %>
  <li><%= button_to game.name, child_game_child_games_path(@child, game_id: game.id) %></li>
  <% end %>
</ul>

<br>
<%= link_to "Choix du profil", children_path %>
